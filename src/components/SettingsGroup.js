import React from 'react'

export const SettingsGroup = (props) => {
    return (
        <>
            <div className="Settings-group">
                {props.children}
            </div>
        </>
    )
}
